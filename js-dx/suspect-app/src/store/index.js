import { createStore } from 'vuex'
import suspect from "@/store/modules/suspect";

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        suspect
    }
})
